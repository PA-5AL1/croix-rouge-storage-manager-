describe('test', () => {
    //let app: express.Application;

    beforeAll(async() => {
        //app = await IntegrationHelpers.getApp();
    });


    it('should except 1+2 to be 3', async () => {
        expect(1+2).toBe(3);
    });

});